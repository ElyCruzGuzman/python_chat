import { StyleSheet } from 'react-native';

export default styles = StyleSheet.create({
  input: {
    backgroundColor: 'red',
    width: 30
  },
  container: {
    flex: 1,
    backgroundColor: '#fff',
    marginTop: 100,
    alignItems: 'center',
    // justifyContent: 'center',
  },
});